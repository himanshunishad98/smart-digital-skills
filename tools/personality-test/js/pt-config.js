/**
 * HEXACO — Frontend Configuration
 * ================================
 * After deploying your Google Apps Script web app,
 * paste the Web App URL below.
 *
 * How to get the URL:
 *   1. In Apps Script → Deploy → New deployment
 *   2. Type: Web App | Execute as: Me | Access: Anyone
 *   3. Click Deploy → copy the URL shown
 *   4. Paste it as the value of GAS_URL below
 */

window.HEXACO_CONFIG = {
  // Centralized Firebase Firestore integration active (leads collection)
  LOCAL_ONLY: false
};
